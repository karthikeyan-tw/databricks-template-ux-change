# Simplest approach - put descriptions directly in enumNames
configurationTemplate:
  type: string
  title: Configuration Template
  enum: [default, alternate]
  enumNames: [
    "📋 Default - Production setup (m4.large, UCX jobs, DLT pipelines, HIPAA/PCI-DSS)",
    "🔬 Alternate - Analytics variant (r5.large, Photon, CloudWatch, cost-optimized)"
  ]
  default: default
  ui:help: |
    **Default:** Complete Thoughtworks production setup with development & demo catalogs, shared cluster, SQL warehouse, comprehensive security and ACLs.
    
    **Alternate:** Analytics-focused setup with larger instances, Photon acceleration, simplified permissions, and cost optimization features. 